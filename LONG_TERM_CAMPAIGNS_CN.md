# 长期演练与多用户组自动加入

## 使用

- 新建活动时勾选“长期演练”，选择一个或多个用户组；系统自动关联这些组，其他配置沿用原有流程。
- 结果页仅展示统计和明细，不再提供长期开关、关联组编辑或手动追加。长期模式与关联组在新建活动时一次确定；已有活动保存的设置继续生效。
- 人员统一在“用户与分组”中维护，结果页不再提供手动追加入口。
- 批量新增沿用“用户与分组”的 CSV 导入功能；保存组成员后，由关联的长期演练自动同步。
- 追加时按整个活动的邮箱去重（忽略大小写和首尾空格）；仅新人生成独立 rid、结果和发送队列记录。
- 自动同步的新人沿用原有投递队列，投递不会早于活动原定启动时间。
- 模板、落地页、URL、SMTP/钉钉配置以及二维码生成逻辑保持原有行为；原有结果、链接与投递记录不覆盖。
- 用户组名称不限，例如“新员工”“上海新入职”“外包新人”等，一个演练可以关联多个组。关联保存后会补入组中尚未参与本活动的人员。
- 此后只需在任意已关联组添加人员，原有后台 worker 约每分钟检查并自动加入队列，无需再次点击追加。
- 关联按组 ID 保存，改组名不影响同步。跨组重复人员按邮箱去重；移出组不会删除历史，再加回也不会重复投递。
- 取消关联或删除组后，不再跟随其后续新增人员，已生成的记录和投递队列保留。删除后新建同名组需要重新关联。
- 多次扫描及服务重启通过已有结果去重，不重复生成 rid 或发送任务；某个活动同步失败会写入日志，其他活动继续处理。
- 自动同步沿用原活动投递配置，且不会早于活动启动时间。
- 结束长期演练使用原有“完成活动”按钮。后端接口保留兼容；关闭长期模式或修改关联组的接口不再从结果页提供入口。
- 保留现有累计统计与导出，本次未新增批次报表或暂停已排队投递的功能。

## 部署

这次修改的是源码和页面，未部署至服务器，也未向实际人员发送消息。
需在原有构建环境重新编译并部署程序，同时部署 templates、修改的 static 文件和 db 下的新迁移文件。
迁移 20260916000000_long_term_campaign.sql 为 campaigns 增加 long_term 字段，已有活动默认关闭。迁移 20260916010000_campaign_auto_groups.sql 新增活动与用户组关联表；旧活动如尚未保存关联组，需通过已认证的关联组接口补齐；本次页面更新不会自动推断旧活动的关联组。启动时由原有迁移机制执行。升级前按现有流程备份数据库与程序。
新接口均使用现有 API 认证与权限控制：
- POST /api/campaigns/{id}/long-term，JSON：{"enabled":true}
- GET /api/campaigns/{id}/auto-groups，查看关联组。
- POST /api/campaigns/{id}/auto-groups，JSON：{"group_ids":[1,2]}；空数组取消所有关联。
- POST /api/campaigns/{id}/recipients，JSON：{"targets":[{"email":"person@example.com"}],"groups":[],"send_date":null}

追加和完成/删除活动通过同一数据库行锁及事务协调，避免并发追加造成重复记录或在活动完成后遗留新增队列。

## 验证

Go 构建成功；models、controllers/api、worker 测试通过。新增测试覆盖开关、权限、重复追加、无效邮箱、多组同步、跨组去重、改名、解除关联、删除组、定时投递、历史保留和已完成活动限制；worker 集成测试验证新人进入投递队列且不重复排队。前端脚本语法检查通过。
数据库测试使用 SQLite；MySQL 迁移及行锁方案未在实际 MySQL 实例验证。未做真实邮件或钉钉投递测试。
